version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: install venv
          command: |
            python3 -m venv venv
            . venv/bin/activate
            python setup.py install
            pip install nose
      - run:
          name: run tests
          command: |
            . venv/bin/activate
            cd ./galaxy_ml
            nosetests ./tests -sv
      - store_test_results:
          path: test-reports/


